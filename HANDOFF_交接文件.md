# HANDOFF 交接文件 · 申悦学习-AI指导物理学习

> 生成时间：2026-09-18（v2，真题修订后）；2026-09-19 更新：月考范围确认、中秋计划 v3 定稿、新增长假专项框架、打印包 v2 定稿归档。本文档供切换模型/新会话快速接手本项目。
> 读完本文件 + `README.md` + `知识库/00_知识库总索引.md` 即可继续工作。

---

## 1. 项目背景与目标

- **服务对象**：家长（很久没接触物理）+ 女儿**申悦**（2026 年 9 月升高二，物理吃力）。
- **近期目标**：中秋假期 **2026.9.25（周五）–9.27（周日）共 3 天**辅导，备战**高二第一次月考**（预计 9.28 当周）。
- **长期目标**：通过本项目建立亲子物理学习交流方式，陪伴到 **2028 年高考**。
- **更大目标**：借月考建立沟通模式——家长当"提问的记者"，孩子当"讲课的小老师"（费曼回讲法）。

## 2. 当前状态（已完成）

| 事项 | 状态 |
|------|------|
| 知识库搭建（exam-study-kb skill 规范） | ✅ 物理 23 卡（思想方法 5 + 概念检验三件套 3）+ 方法 2 卡 + 状态表 + 索引 + 卡片模板 v2（`知识库/01_知识卡片模板.md`） |
| **四校真题入库与月考范围修订（v2）** | ✅ 分析报告 + 静电场 6 卡 + 光学 2 卡 + 振动波 1 卡；PDF 归档于 `知识库/物理/素材与拓展/真题试卷/`（含提取文本） |
| **学而思讲义入库（v4）** | ✅ 《电学学习资料（教师版）》52 页归档 `素材与拓展/讲义资料/`；补静电平衡/静电屏蔽缺口；新建"概念检验"维度（过关 30 问自测卷） |
| **卡片结构升级 v2（学-讲-练-复盘闭环）** | ✅ 13 张物理卡均含：核心内容 + 🗣️费曼讲解 + 📝解题示范（真题原型题）+ ✅复盘要点自测清单 |
| **打印支持** | ✅ 每张卡片页右下角"🖨️ 打印本页"按钮 + 打印样式（隐藏导航/页脚、防表格断页、A4 边距），供碎片化复习 |
| **中秋打印包 PDF（v6）** | ✅ `打印包/中秋打印包_物理.pdf`（13 页 A4，四部分：概念检验 30 问 ×3 + 陷阱卡 ×2 + 思想方法速查表 ×5 + **家长提问卡 30 问**），reportlab 生成；生成器 `build_print_pack.py`（改卡片后重跑即可再生成）；预览拼图 `打印包/预览_contact-sheet.png`；构建时同步到 `docs/print/`，网站首页有下载入口 |
| **家长提问卡 + 复习日历 + 错题入库（v6）** | ✅ `家长支持/家长提问卡_费曼回话题库.md`（口试 30 问，与检验卷笔试互补）、`知识库/复盘追踪/艾宾浩斯复习日历_中秋起.md`（1/2/4/7/15 天打卡表）、`家长支持/错题入库速查_考后三步.md`；公式一律用 `$...$`（Unicode 上下标在 PDF 字体中缺字形，网站/PDF 双端兼容） |
| **周复盘定时任务** | ✅ 「物理知识库 · 每周日晚复盘」每周日 20:17 Asia/Shanghai，local_conversation，复盘后自动 build + push（automation_838b11e7，由旧任务改造） |
| 中秋 3 天辅导计划 v3 定稿（静电场主线，范围已确认） | ✅ `中秋3天辅导计划_9月25-27日.md` |
| 长假专项复习设计框架（国庆待用） | ✅ `家长支持/长假专项复习设计框架.md`（3 天/7 天模板 + 五步法） |
| 家长物理速成指南（静电场篇 + 动量篇） | ✅ `家长支持/` 两份 |
| GitHub Pages 静态网站 | ✅ `build_site.py` 构建到 `docs/`（38 页），全部页面 vite 实测 HTTP 200；已上线 https://goodniuniu.github.io/shenyue-physics/ |
| vite 中文路径 GBK 修复 + HTML 标题转义修复 | ✅ `vite.config.js`、`build_site.py`（page_html 已加 html.escape） |
| GitHub Pages 部署说明 | ✅ `部署说明_GitHubPages.md` |

**注意：已推送 GitHub（git@github.com:goodniuniu/shenyue-physics.git，SSH 走 443 端口+本地代理 7897，首次大推送慢属正常）。**

## 3. 关键假设（2026-09-19 更新）

1. **月考范围已确认 = 四校真题范围**：必修三《静电场》为主干 + 选必一已学章节（动量/振动与波/光，因校而异）（2026-09-19 用户确认）。
   - 依据：2025.10 华附（静电场+光）、执信（纯静电场）、铁一（振动波光+动量压轴）、二中（动量为主）四套真题。静电场是四校最大公约数。
   - 详见 `知识库/物理/素材与拓展/高二深化_物理_素材与拓展_广州四校高二上月考真题分析报告.md`。
   - 中秋计划已 v3 定稿：Day2 下午机动线仍由 Day1 早上 5 分钟确认学校进度选定；**说不清进度则走默认线（静电场深化）**。
   - **运作模式已确立**：中秋三天是"长假专项复习"首期实例；其余内容为常态化资源储备；国庆等长假按 `家长支持/长假专项复习设计框架.md` 设计下一期专项（国庆专项待月考分析后生成）。
2. 教材版本按人教版假设；省份按广东（等级赋分）假设。
3. 选科组合未确认——知识库目前只有物理 + 方法维度。

## 4. 目录结构与文件职责

```
中秋3天辅导计划_9月25-27日.md          # 源：3天计划 v2（改完需重新构建网站）
家长支持/家长物理速成指南_静电场篇.md    # 源：家长指南（第一优先，含打印速查表）
家长支持/家长物理速成指南_动量篇.md      # 源：家长指南（动量线备用）
知识库/                              # 源：全部知识卡片 Markdown
  00_知识库总索引.md / 物理/索引_物理.md / 方法/索引_方法.md
  物理/{核心知识网络,典型题型与方法,易错警示与辨析,素材与拓展}/
  物理/素材与拓展/真题试卷/           # 4 校真题原卷+解析 PDF，提取文本/ 子目录
  方法/{学习方法,考试策略,心理建设,生理管理}/
  复盘追踪/知识掌握状态表.md
build_site.py                       # Markdown → docs/ 静态站（markdown-it-py，禁用 linkify，标题/描述已 HTML 转义）
build_print_pack.py                 # 知识卡片 → 打印包/中秋打印包_物理.pdf（reportlab，Unicode 上下标用 <sub>/<super>，emoji 映射为中文标签）
打印包/                             # 打印包 PDF + 预览_contact-sheet.png（不进 docs/）
docs/                               # 生成的网站（GitHub Pages 发布此目录，含 .nojekyll）
vite.config.js                      # 修复 GBK 编码请求导致 vite "URI malformed"
package.json                        # dev: vite docs --config vite.config.js
部署说明_GitHubPages.md / README.md / .gitignore
```

**维护流程**：改任何 Markdown 源 → `python build_site.py` → `docs/` 重新生成（整个目录先删后建，`vite.config.js` 在根目录不受影响，无需放入 docs）。

## 5. 技术要点与坑（新模型必读）

1. **npm/node 不在 PATH**：用 Kimi 内置运行时的完整路径调用
   `/c/Users/admin/AppData/Local/Programs/kimi-desktop/resources/resources/runtime/npm.cmd`（同目录有 `npx.cmd`、`node.exe`；注意是 `admin` 不是 `user`，旧版交接写错了）。PATH export 可能不生效，直接全路径调用最稳。
2. **vite 默认端口 5173**（dev 脚本未固定端口）；Kimi 客户端预览卡会用逻辑端口 7100 转发 CLI 参数。本地验证时从 vite 输出日志读实际端口。
3. **vite GBK 问题**（已修，勿删 `vite.config.js`）：Windows 客户端会把中文 URL 先转 GBK 再 percent-encode，vite `decodeURI()` 抛 "URI malformed"（500）。中间件已做 UTF-8→GBK 自动识别修复。
4. **卡片标题禁含 ASCII 双引号**：会撑破 `<meta content="...">` 导致 vite 500。build_site.py 已加 html.escape 兜底，但 Markdown 标题请用「」。
5. **杀进程要杀树**：`netstat -ano | grep ":<port>"` 找 PID 后 `taskkill //F //T //PID <pid>`。**绝不留服务器运行**。
6. **Windows 控制台 GBK**：Python 脚本避免 print 中文/emoji；文件读写一律 `encoding="utf-8"`。
7. markdown-it-py 必须 `.disable("linkify")`（环境未装 linkify-it-py）。
8. KaTeX 已**本地化**（`assets/katex/` 随构建拷入 `docs/assets/katex/`，只保留 woff2 字体约 608K），不再依赖 jsdelivr CDN（国内被墙导致公式不渲染）。KATEX 模板用 `__BASE__` 占位符（不能 `.format()`，auto-render 配置里有花括号）。离线也能渲染。
9. 卡片间链接是中文相对路径 `.md`，构建时统一重写为 `.html`（fix_links），目录镜像保证相对路径有效。
10. PDF 提取文本中公式符号大量丢失（图片/特殊字体），只可用于检索考点，做题用原卷 PDF。
11. **reportlab 打印包三坑**（`build_print_pack.py` 已处理）：CJK 字体缺 Unicode 上下标字形 → 用 `<sub>/<super>` 标签；✅⚠️❌ 等 emoji 缺字形 → conv() 映射为"已掌握/待强化/未理解"；含 HTML 标签的标题要先包 `<b>` 再过 conv（conv 会转义）。质检用 kimi-pdf skill 渲染逐页目检。
12. **kb 页面相对路径 = `"../" * len(rel.parts)`**（页面在 docs/kb/ 下，kb/ 本身也算一层）；`kb/index.html` 用 `"../"`。曾因少算一级导致全部知识库页面样式+KaTeX 404，公式显示原始 `$...$`（2026-09-18 修复）。
13. **定时任务**：「物理知识库 · 每周日晚复盘」（automation_838b11e7，每周日 20:17 Asia/Shanghai，local_conversation，复盘后自动 build_site.py + push）。由旧的"2028广东新高考知识库·周复盘"改造而来。

## 6. 常用工作流（用户会直接下的指令）

- "入库物理错题/试卷"（附照片或文件）：按 exam-study-kb skill 的 PDF/试卷入库流程建卡 → 更新索引与状态表 → 重跑 `build_site.py`。
- "查物理+关键词" / "查方法+关键词"：检索知识库卡片。
- "本周知识库复盘"：跑 skill 里的五步复盘流程，更新 `知识掌握状态表.md`，输出下周计划。
- 新增内容后提醒：`python build_site.py` 同步网站。

## 7. 待办与下一步（按优先级）

1. ~~确认月考范围~~ ✅ 已确认（四校真题范围，2026-09-19）。仍待确认：①教材版本 ②选科组合。
2. 假期中用户可能回来：录入诊断/模拟错题 → 建卡 → 更新状态表。
3. 考后（9.28 当周）：月考试卷入库分析，校准"四校真题假设"与实际考试的偏差。
4. **国庆专项（10.1–10.7）**：月考分析完成后，按 `家长支持/长假专项复习设计框架.md` 生成国庆专项计划。
5. ~~每周复盘定时任务~~ ✅ 已建（每周日 20:17，见第 5 节第 13 条）。
6. GitHub Pages 发布：用户给账号后按其 `部署说明_GitHubPages.md` 操作，或指导用户自行三步发布。

**杂项备忘（2026-09-19）**：打印包 v2 已定稿为正式版（质检截图与 _v2.pdf 归档在 `.verify_tmp/`，未删除）；复习日历 10.6 原误标"中秋节团圆"已改普通休整（2026 中秋为 9.25，假期第一天）。

## 8. 沟通基调备忘

- 家长物理零基础：解释概念必须用生活类比（安全气囊、冰面互推、山坡海拔、攒钱），避免公式轰炸。
- 强调"关系先于成绩"、夸行为不夸聪明；假期每天学习约 4 小时，晚上只留 15 分钟回讲。
- 所有面向用户的回复用中文；文件命名遵循 `[时间标签]_[科目]_[维度]_[知识点].md` 规范。
